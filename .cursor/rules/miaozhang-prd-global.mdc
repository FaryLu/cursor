---
description: 秒账 PRD 写作、历史检索、影响模块分析与需求文档生成的全局约束。
alwaysApply: true
---

## 秒账 PRD 全局规则

1. 执行优先级：必须先读取共享前置上下文，再按步骤调用 Skill，最后生成 PRD；不跳过任何一步，不凭空生成内容。
2. 依赖规则：后一个 Skill 必须完全依赖前一个 Skill 的输出结果，层层递进，按「检索历史 → 分析影响模块 → 生成 PRD →（可选）细化 PRD」顺序执行。
3. 内容规则：所有输出必须基于秒账本地历史文档、MCP 历史记录和影响模块模板；术语、字段名、模块名、业务流程与秒账现有规范保持一致，不新增无依据的模块、字段、逻辑。
4. 产品设计规则：严格遵循「从大到小」逻辑，先确定受影响模块，再梳理功能范围，再拆解现有流程，最后明确字段逻辑和业务规则，贴合 ToB 进销存用户（商家、财务、库管）使用场景。
5. 参数提取规则：自动从用户输入的「需求标题 + 描述」中提取关键词、模块名，不缺参、不瞎编，参数贴合秒账业务场景。
6. 输出规则：最终 PRD 必须符合秒账标准模板；语言正式、可落地，适配 ToB 研发对接需求。

## 共享引用

凡 PRD 生成任务，必须先读取：
- @skills/_shared/prd-context.md
- @skills/_shared/mcp-retrieval.md
- @skills/_shared/output-convention.md

## 文档档位

不同步骤产出不同质量门槛，不得混用：

| 档位 | 对应步骤 | 必须包含 | 不要求 |
|------|----------|----------|--------|
| 影响模块 | search-history-docs | 直接/间接/横切三层、关联理由、补漏检查 | 页面字段细节 |
| 标准骨架 PRD | generate-final-prd | 需求背景、影响模块、模块/页面/区域结构、关键字段与按钮、关键规则、P0 验收标准 | 全量字段级、逐按钮展开 |
| 细化版 PRD | refine-prd-detail | 骨架 PRD 全部内容 + 字段/按钮/权限/数据范围逐条展开 | 重新做影响模块分析 |

## 必须遵守

- 必须先检索历史文档，不允许凭空设计新功能。
- 术语、字段名、流程名称必须与历史文档保持一致。
- 新需求不能与现有功能逻辑冲突，冲突必须标注并给出兼容方案。
- 不允许编造不存在的模块、字段、流程。
- 字段、交互、规则必须贴合现有系统逻辑，不做天马行空设计。
- 必须按顺序执行：检索 → 分析影响模块 → 生成 PRD →（用户明确要求时）细化。
- 不得跳过任何一步，不得省略关键信息。
- 新增功能若涉及全局业务行为，必须检查商户设置的业务属性、货品属性、偏好设置，不得影响老用户的既有操作。
- 凡 PRD 生成任务，必须读取 `@.cursor/rules/miaozhang-product-knowledge.mdc`，不依赖文件 globs 是否触发。

## 输出要求

- 使用中文编写，语言简洁专业。
- 标准骨架 PRD：每个主流程功能点至少提供 P0 验收标准；细化版 PRD：关键页面逐条提供可验收标准。
- 考虑边界情况和异常处理。
- 区分功能优先级（P0/P1/P2）。
- 必要时包含流程图或状态说明。
- 生成逻辑复杂时，建议带一个示例。

## 禁止事项

- 输出模糊不清的描述。
- 遗漏关键业务规则。
- 忽略权限、数据范围和数据安全。
- 使用过于技术化的实现细节替代产品规则。
- 用细化版 PRD 的标准要求标准骨架 PRD，或在骨架阶段强行全量展开所有字段。
